import React from "react"

export default () => (
    <nav className="nav">
        <a href="#resume">Resume</a>
        <a href="contact">Contact</a>
        <a href="projects">Projects</a>
    </nav>
)